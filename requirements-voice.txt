# Install into the project's .venv with uv pip install -r requirements-voice.txt
faster-whisper==1.2.1
mlx-whisper==0.4.3; sys_platform == "darwin" and platform_machine == "arm64"
kokoro-onnx==0.4.9
sherpa-onnx==1.13.8
